/*Yilin Luo;
 * NetID: yluo21;
 * Homework9;
 * Lab:Mon & Wed 2:00-3:15pm;
 * "I did not copy code from anyone on this homework";
*/
public class AnswersForQuestion1 {
	//(a)not valid, because j and i have set values which are not equal.
	//(b)not valid, because i cannot be initialized twice;
	//(c)valid;
	//(d)valid;
	//(e)not valid. because the for loop can't be initialized two times.
	//(f)valid. 
	//(g)valid;
}
